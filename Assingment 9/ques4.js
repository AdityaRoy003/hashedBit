// 4. Square Each Number Using map()


function squareNumbers(numbers) {
    return numbers.map(num => num * num);
  }
  
  const nums = [1, 2, 3, 4, 5];
  console.log("Squared numbers:", squareNumbers(nums));

  